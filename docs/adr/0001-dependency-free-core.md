# 0001: Dependency-free core

Status: accepted.

The core uses only the Python standard library. Optional model support must remain an extra so
installation, import, and offline behavior stay predictable.
